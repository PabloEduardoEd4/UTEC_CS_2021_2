#include <iostream>
#include "funciones.h"

int main()
{
	/*
	float promedio = 0.0, mayor = 0.0, menor = 0.0;
	int alumnos = ingresarNumeroAlumnos();
	float *pNotas = ingresarNotasAlumnos(alumnos);
	calcularEstadisticos(pNotas, alumnos, promedio, mayor, menor);
	mostrarEstadisticos(promedio, mayor, menor);
	mostrarPromedioElimandoMenor(pNotas, alumnos, menor);
	delete[] pNotas;
	*/
	return 0;
}
