#include <iostream>
#include <string>

int main(int argc, char *argv[])
{

    char div[] = "=====================================================";
    /*
    // Arrays in c++ work difrently, just being glorfied pointers
    int arr[] = {1, 2, 3, 4, 5},
    *p = arr

    for (int x=0
         x < 10
         x++)
    {
        // When outside the range they print the values of other adresses
        std:: cout << arr[x] << std: : endl
        std:: cout << p[x] << std: : endl
        std: : cout << (p[x] == arr[x]) << std: : endl
    }
    std:: cout
    for (int x=0
         x < 10
         x++)
    {
        // When outside the range they print the values of other adresses
        std:: cout << arr[x] << std: : endl
        std:: cout << p[x] << std: : endl
        std: : cout << (p[x] == arr[x]) << std: : endl
    }

    std:: cout << div << std: : endl
    // The size of a array does not have a particular variable
    // There are two ways, the sizeof form
    std:: cout << sizeof(arr) / sizeof(arr[0]) << std: : endl
    std:: cout << *(&arr + 1) - arr << std: : endl
    std:: cout << (*(&arr + 1) - arr == sizeof(arr) / sizeof(arr[0])) << std: : endl

    std:: cout << div << std: : endl
    // Due to the speed of c++ this genrally does not matter though typically
    // mathematical solutions are faster than functions

    // Using this we can define the size of the array
    int size = *(& arr + 1) - arr

    // Now we can use this to limit the array print
    for (int x=0
         x < size
         x++)
    {
        std: : cout << arr[x] << std: : endl
    }

    // Using pointers allows you to define arrays latter and
    // define them to already declared variables

    // Class lesson ex
    int number
    std:: cout << "Ingrese numero: "
    std:: cin >> number
    std:: cout
    // Equivalent to number == 0
    if (!number)
    {
        std: : cout << "Numero es zero"
    }
    // Equivalent to number % 2 == 0
    else if (number ^ 1)
    {
        std: : cout << "Numero es par"
    }
    else
    {
        std: : cout << "Numero es impar"
    }

    int month
    char arr[][11] = {"Junio", "Julio", "Agosto", "Septiembre"}
    std: : cout << "[1] Junio\n"
    << "[2] Julio\n"
    << "[3] Agosto\n"
    << "[4] Septiembre\n"
    std:: cout << ">> "
    std:: cin >> month
    std:: cout

    // Version switch
    switch(month)
    {
        case 1:
        std:: cout << "Junio"
        break
        case 2:
        std:: cout << "Julio"
        break
        case 3:
        std:: cout << "Agosto"
        break
        case 4:
        std:: cout << "Septiembre"
        break
        default:
        std: : cout << "Invalido"
    }
    std: : cout << '\n'
    << div << std:: endl

    month--
    int size = *(& arr + 1) - arr
    if (0 < month < size)
    {
        std: : cout << arr[month] << std: : endl
    }
    else
    {
        std: : cout << "Invalido" << std: : endl
    }
    * /

    // Iterator, for
    for (int x=0
         x < 10
         x++)
    {
        std: : cout << "C++" << std: : endl
    }
    std:: cout << div << std: : endl
    int k = 0
    while (k < 5)
    {
        std:: cout << "C++" << std: : endl
        k++
    }
    int a = 2.3
    double b = 1.0
    std: : cout << a << b
    */
}
